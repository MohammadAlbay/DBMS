namespace dbms.sql
{
    public static class SqlParser
    {
        public static SQLBlock Parse(string sql) 
        { 
            SQLBlock block = new();


            return block;
        }

    }
}